# ionic-capacitor-camera-preview-demo

## Installation
- Clone the project ```https://github.com/dotmarn/ionic-capacitor-camera-preview-demo.git```

- Install npm dependencies ```npm install```

- Start the project: ``` ionic serve ```
